.content-box
  %h2 #{@contact.try(:name)} / #{@service.name}
= admin_form_for [:admin, @service.location, @service, @contact], html: { class: 'edit_entry' } do |f|
  = render 'admin/contacts/form', f: f
