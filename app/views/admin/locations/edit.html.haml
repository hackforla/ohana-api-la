.content-box
  %h2= @location.name == @org.try(:name) ? @location.name : "#{@org.try(:name)} / #{@location.name}"
= admin_form_for [:admin, @location], html: { class: 'edit_entry' } do |f|
  = render 'form', f: f

